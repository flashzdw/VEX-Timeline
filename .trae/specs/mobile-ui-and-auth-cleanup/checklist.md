# 手机端 UI 适配与登录/注册页调试信息清理 - 验证清单

## 调试信息清理验证
- [x] 登录/注册页 DOM 中 `#diagnostic-bar` 元素已删除
- [x] 登录/注册页不显示 URL / Key / Session / Token / 时间轴 五行调试信息
- [x] `#diag-banner` 改为用户友好的小号灰色提示（或直接隐藏）
- [x] `app.js` 中 `renderDiagnosticBar()` 在 `#diagnostic-bar` 不存在时安全 no-op，不抛 null 引用
- [x] 控制台中诊断信息（`console.warn`）保留，开发者仍可调试
- [x] 登录、注册、登出流程不报错

## 登录/注册页手机端适配验证
- [x] 375px（iPhone SE）视口下无横向滚动
- [x] 390px（iPhone 14）视口下表单居中、按钮可达
- [x] 768px（iPad）视口下布局正常，向桌面布局过渡
- [x] 输入框高度 ≥ 48px（实际 56px = h-14）
- [x] 登录/注册按钮高度 ≥ 48px
- [x] 字号未触发 iOS 自动放大（输入框字号 ≥ 1rem，加 `text-base`）
- [x] 视口高度 < 700px 时表单可垂直滚动（`overflow-y-auto`）
- [x] 横屏（iPhone 横屏）下表单仍可正常使用

## 主界面 header 与汉堡菜单手机端适配
- [x] header 高度在 < 768px 时为 64px（h-16，`h-16 md:h-20`）
- [x] 移动端汉堡菜单包含：视图切换、时间轴选择、赛队操作、登出
- [x] 移动端汉堡菜单包含"云状态"与"添加记录"入口（新增"快捷操作"区块）
- [x] 抽屉菜单项高度 ≥ 48px（统一 `h-12`）
- [x] 375px 视口下 header 不溢出
- [x] 所有 header 可点击元素 ≥ 40px

## 模态框手机端适配验证
- [x] 添加/编辑记录模态框在 < 768px 时全屏显示（无圆角、贴边）
- [x] 创建赛队时间轴模态框在 < 768px 时全屏显示
- [x] 邀请码显示模态框在 < 768px 时全屏显示，邀请码字号 ≤ 2rem（实际 1.5rem）
- [x] 加入赛队时间轴模态框在 < 768px 时全屏显示
- [x] 日内记录详情模态框在 < 768px 时全屏显示，图片最大宽度 = 视口 - 2rem（`max-w-full`）
- [x] 所有模态框关闭按钮 ≥ 44px（`.vx-modal .h-10.w-10` min-width/min-height 44px）
- [x] 模态框打开/关闭动画在窄屏下正常

## 时间轴与月历手机端适配验证
- [x] 月历单格在 < 640px 时 `min-height` = 72px
- [x] 月历在 375px 视口下首屏可见 6 周
- [x] 时间轴卡片在窄屏下内边距合适（`0.375rem 0.5rem` = 6px 8px）
- [x] 过滤器按钮在窄屏下 ≥ 40px（`h-10` = 40px）
- [x] 时间轴卡片标题在窄屏字号合适

## 触摸目标规范验证
- [x] 关键按钮（添加记录、登录、注册、过滤、重要性选择、月历导航）≥ 48px
- [x] 次要按钮（云状态、用户菜单、汉堡）≥ 40px
- [x] 输入框高度 ≥ 48px

## 桌面端回归验证
- [x] ≥ 1024px 视口下登录页左侧 Hero 分栏正常显示（`hidden lg:flex`）
- [x] ≥ 1024px 视口下主界面 header 单行布局正常（`h-16 md:h-20`）
- [x] ≥ 1024px 视口下模态框仍为居中弹窗样式（`@media (max-width: 767px)` 仅作用于小屏）
- [x] ≥ 1024px 视口下月历单格 130px 高（Round 7 设计，未变更）

## 不破坏既有功能
- [x] 登录/注册/登出流程正常工作
- [x] 时间轴与月历渲染正常
- [x] 记录增删改查正常
- [x] 赛队创建/加入/管理正常
- [x] 云端同步指示器在 header 与移动端抽屉中均可触发
